json格式
const payload = {
    type: 1,// 1是电脑传给硬件的数据，包括步频和配速
    cadence: cadence,
    pace_min: paceMin,
    pace_sec: paceSec
};

const payload = {
    type: 2,// 2是硬件传给电脑的数据，包括时间和距离
    time: time,
    distance: distance
};