#ifndef __ROTATE_H__
#define __ROTATE_H__

// 声明外部变量，供其他文件访问
// 使用 volatile 确保多任务/中断环境下变量更新的可见性
extern volatile int standard_speed;

// 这里可以添加 rotate 相关的函数声明
// void rotate_init();
// void rotate_loop();

#endif

