# Python Backend Refactoring Plan

## 1. Environment Verification
- Check Python availability and version.
- Verify existing Node.js files to be removed.

## 2. Backend Implementation
- Create `requirements.txt` with necessary dependencies (Flask).
- Create `server.py` using Flask to replace `server.js`.
  - Configure to serve static files from `public/` directory.
  - Implement SPA fallback routing (redirect 404s to index.html).
  - Set port to 2333 to match previous configuration.

## 3. Cleanup
- Remove Node.js related files:
  - `server.js`
  - `package.json`
  - `package-lock.json` (if exists)
  - `node_modules/` directory (optional but recommended for clean refactor)

## 4. Verification
- Start the Python server to verify it works correctly.
- Check if frontend loads correctly via the new backend.
